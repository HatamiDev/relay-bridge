<?xml version="1.0" encoding="utf-8"?>
<!--
    The single application manifest.

    Permissions and components from :gateway and :client are merged in
    automatically. Everything declared here is what there can only be one of:
    the Application class, the launcher activity, the icon, the theme, and the
    single FirebaseMessagingService that dispatches by role.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".RelayApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.RelayClient"
        android:usesCleartextTraffic="false"
        android:networkSecurityConfig="@xml/network_security_config"
        android:enableOnBackInvokedCallback="true">

        <!--
            The only launcher entry point. It reads the stored role and forwards
            to the role picker, the gateway console, or the receiver UI. It is
            `noHistory` so backing out of a forwarded screen exits the app
            rather than bouncing through an empty router.
        -->
        <activity
            android:name=".RouterActivity"
            android:exported="true"
            android:noHistory="true"
            android:theme="@style/Theme.RelayClient">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- First-launch role picker: Sender (SIM) or Receiver. -->
        <activity
            android:name=".RoleSelectActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:theme="@style/Theme.RelayClient" />

        <!-- One FCM service for the whole APK; it dispatches by stored role. -->
        <service
            android:name=".RelayMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.firebase.messaging.default_notification_channel_id"
            android:value="relay_service" />

    </application>
</manifest>
