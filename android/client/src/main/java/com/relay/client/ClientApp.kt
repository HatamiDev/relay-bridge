package com.relay.client

/*
 * Intentionally empty.
 *
 * The merged APK has exactly one Application class: `com.relay.app.RelayApp`.
 * Everything this file used to do — channel creation, FCM token resolution,
 * starting the relay service — now happens there, gated on the stored role.
 */
