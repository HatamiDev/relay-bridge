package com.relay.client.ui.components

/*
 * Intentionally empty — this file holds no declarations.
 *
 * The pinned-contacts row was rebuilt as `StoryRow.kt` when the visual language
 * moved to the aurora gradient: the card is now a squircle with a blurred photo
 * backdrop, an unseen/seen ring convention, and a leading "Your story" action.
 *
 * The row composable and its item type live there under their new names. This
 * stub only exists so the old path does not read as a deletion accident; it can
 * go the next time this package is touched.
 */
