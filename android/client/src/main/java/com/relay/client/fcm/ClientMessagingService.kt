package com.relay.client.fcm

/*
 * Intentionally empty — see the note in
 * `com.relay.gateway.fcm.GatewayMessagingService`.
 *
 * The single FirebaseMessagingService for the merged APK is
 * `com.relay.app.RelayMessagingService`.
 */
